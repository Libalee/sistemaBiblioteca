spring:
  datasource:
    url: jdbc:mysql://localhost:3306/library_database
    username: root
    password: senhamtlegal24
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: false